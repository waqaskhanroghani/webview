import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  mainText: {
    color: 'red',
    fontSize: 15,
    alignSelf: 'center',
    height: '8%',
  },
});

export default styles;
